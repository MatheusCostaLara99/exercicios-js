/* 3)​ Crie uma função que recebe dois parâmetros, 
base e expoente, e retorne a base elevada ao expoente. */

let potencia = (base,expoente)=>{
    return Math.pow(base,expoente)
}

console.log(potencia(2,4))
